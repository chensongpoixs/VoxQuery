# ============================================================
# Docker Compose GPU 绑定覆盖文件
# Profile: single-gpu (单张 RTX 5080 (16GB) 部署全部模型，适合开发测试环境)
# GPU: 1×NVIDIA RTX 5080
# 自动生成，请勿手动编辑
# ============================================================

services:
  llm-service:
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                device_ids: ["0"]
                capabilities: [gpu]

  embedding-service:
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                device_ids: ["0"]
                capabilities: [gpu]

  asr-service:
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                device_ids: ["0"]
                capabilities: [gpu]

  tts-service:
      deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                device_ids: ["0"]
                capabilities: [gpu]
